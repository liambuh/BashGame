BashGame is a simple grid/turn-based game written in shell scripts, and playable in the linux bash shell.

To run the game, use the command 'bash ./main.sh'

Movement is done via the vim keys (h,j,k,l)
To quit, press q (or simply ctrl+c)

This game is not finished, so not many features are added yet.